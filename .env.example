# Django settings
SECRET_KEY=your-secret-key-here
DEBUG=False
ALLOWED_HOSTS=.railway.app,localhost,127.0.0.1

# CORS settings
CORS_ALLOW_ALL_ORIGINS=False
# For multiple origins, separate with commas (no spaces)
# Each origin must include scheme (http:// or https://)
CORS_ALLOWED_ORIGINS=https://your-frontend-app.railway.app,http://localhost:3000

# Database settings
# This will be automatically set by Railway
# DATABASE_URL=postgres://username:password@hostname:port/database_name
